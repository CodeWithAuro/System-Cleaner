# System-Cleaner 🧹
A simple technical tool to bridge Python and Bash.

### How it works
1. Uses Python's `subprocess` module.
2. Triggers a `find` command in Bash to identify heavy files.
3. Helps developers keep their workspaces clean.

### How to Run
```bash
python3 cleaner.py
